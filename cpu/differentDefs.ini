[functionName]
definition =
    cpuSpecificName ( )

#! Indirection needed due to current ambiguity in syntax
[functionCall]
definition = 
    cpuSpecificName();

[print_sum]
args = x,y
definition =
    std::cout << x << " + " << y << " = " << x+y;
    std::cout << std::endl;
